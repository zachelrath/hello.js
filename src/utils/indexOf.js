//
// indexOf
// IE hack Array.indexOf doesn't exist prior to IE9
//
define(function(){

	return function(a,s){
		// Do we need the hack?
		if(a.indexOf){
			return a.indexOf(s);
		}

		for(var j=0;j<a.length;j++){
			if(a[j]===s){
				return j;
			}
		}
		return -1;
	};

});